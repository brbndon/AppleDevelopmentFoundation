# Contributing

Keep changes focused, add a public API only when consumers need it, and run `./Scripts/verify.sh` before proposing a reusable addition. See `AGENTS.md`, `Documentation/ModuleBoundaries.md`, and `Documentation/TestingGuide.md` for the durable rules.
