---
name: swift-testing-verification
description: Use after a meaningful Apple-platform code change to select and run verification. Do not use for planning-only work.
---

# Swift testing and verification

Determine the smallest relevant tests; run module tests and package-wide tests when dependencies or public APIs change; build affected iOS and macOS examples. Report exact commands/outcomes and distinguish existing failures from introduced failures. Never claim checks passed without executing available checks.
